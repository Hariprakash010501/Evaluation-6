import Head from 'next/head'
import profStyle from '@/styles/Home.module.css'
import Education from './education'
import Projects from './projects'
import profileData from '@/Config/datas'
import Link from 'next/link'
import Image from 'next/image'
import img from '@/images/picture.jpeg'
import { useRouter } from 'next/router'
export default function Home() {
  const router = useRouter();
  return (
    <>
      <Head>
        <title>Portfolio</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </Head>
      <div style={{ backgroundColor: "rgb(51,50,50)" }}>
        <div className={profStyle.grid} >
          <div className={profStyle.gridItem1}>

            <Image style={{ borderRadius: "50%" }} src={img} alt="hi" width={120} height={120} />
            <h2>Hari Prakash</h2>
            <Link style={{ textDecoration: "none", color: "white" }} href='https://github.com/Hariprakash010501'>@Hariprakash010501</Link>
            <br /> <br /><h2>Software Engineer</h2><br />
            <h4 className={profStyle.button} onClick={() => router.push(profileData.info.resume)}>Resume</h4><br />
            <h4 className={profStyle.button} onClick={() => router.push('https://github.com/Hariprakash010501')}>Follow</h4>
          </div>
          <div className={profStyle.gridItem2}><Projects /></div>
          <div className={profStyle.gridItem2} >
            {profileData.info.techstack.map((data) => (<h3 key={data}>{data}</h3>))}
          </div>
          <div className={profStyle.gridItem2}>
            <Education />
          </div>
        </div>
      </div>
    </>
  )
}
